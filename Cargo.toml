[package]
name = "tapes"
version = "0.1.0"
edition = "2024"

[dependencies]
slab = { version = "0.4" }
blake2 = { version = "0.10" }
borsh = { version = "1", features = ["derive"] }
parking_lot = { version = "0.12" }
bytemuck = { version = "1" }
libc = "0.2.179"

[dev-dependencies]
tempfile = "3"
